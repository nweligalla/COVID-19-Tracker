import 'package:flutter/material.dart';

const backgroundColor = Color(0xFFECEFF1);

//Reusable card
const reusableCardValueStyle = TextStyle(
  fontSize: 30,
  fontWeight: FontWeight.bold,
);

const reusableCardTextStyle = TextStyle(fontSize: 23, color: Colors.white);

//Rounded button

const roundedButtonTextStyle =
    TextStyle(color: Colors.white, fontSize: 17, fontWeight: FontWeight.bold);

//main screens

const mainTitleTextStyle =
    TextStyle(fontSize: 30, color: Colors.white, fontWeight: FontWeight.bold);

const lastUpdateTextStyle = TextStyle(
    color: Color(0xFF546E7A),
    fontSize: 15,
    fontFamily: "OpenSans",
    fontWeight: FontWeight.bold);

const cardTitleTextStyle = TextStyle(
    color: Color(0xFF424242),
    fontSize: 25,
    fontWeight: FontWeight.bold,
    fontFamily: "Muli");
